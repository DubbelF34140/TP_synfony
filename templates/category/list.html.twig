{% extends 'base.html.twig' %}

{% block body %}
    <h1>Category List</h1>
    <ul>
        {% for category in categorys %}
            <li>
                <strong>{{ category.name }}</strong>
            </li>
        {% endfor %}
    </ul>

{% endblock %}

{% block title %}

{% endblock %}

